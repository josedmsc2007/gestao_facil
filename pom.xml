<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- O "pai" do projeto. Herdando dele, o Spring Boot ja define as versoes
         compativeis de todas as bibliotecas abaixo: por isso nenhuma
         dependencia precisa declarar <version>. -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.gestaofacil</groupId>
    <artifactId>gestao-facil</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>gestao-facil</name>
    <description>Sistema de controle de veiculos - Gestao Facil</description>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <!-- Spring Web: servidor Tomcat embutido + controllers (@Controller) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Data JPA: mapeia as classes Java para as tabelas do banco -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Thymeleaf: motor de templates HTML das telas -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>

        <!-- Spring Security: autenticacao, perfis de acesso e hash de senha -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Validacao (@NotBlank, @Size...) usada nos formularios das proximas telas -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Driver do PostgreSQL: so existe quando o programa esta rodando -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- DevTools: reinicia a aplicacao sozinho quando um arquivo muda -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- Ferramentas de teste (JUnit, Mockito). So valem em src/test -->
        <!-- H2: banco em memoria usado APENAS pelos testes automatizados.
             Assim "mvnw test" roda em qualquer maquina do grupo, sem precisar
             do PostgreSQL no ar nem da senha em application-local.properties,
             e sem sujar o banco de desenvolvimento. A configuracao esta em
             src/test/resources/application.properties. Para rodar os testes
             contra o PostgreSQL de verdade, apague esta dependencia e aquele
             arquivo. -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Plugin que permite rodar com "mvn spring-boot:run" e gerar o .jar -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
